// const MAIN_HOST = (process.env.NODE_ENV === 'development' || process.env.IS_DEV) ? 'mfwdev.com' : 'mafengwo.cn'
const MAIN_HOST = 'localhost:8081'
const UTIL = {
  AJAX_BASEURL: 'http://' + MAIN_HOST
}
export default UTIL